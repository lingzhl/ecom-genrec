[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ecom-genrec"
version = "0.1.0"
description = "Large-evaluation generative recommendation pipeline for e-commerce."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
